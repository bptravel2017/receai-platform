# 2026-03-22

- Inspected `/Users/bptravel/.openclaw/workspace/projects/receai-platform` to map framework, folder structure, UI pages, business logic, API routes, and database handling.
- Found a small Next.js 16 + React 19 + TypeScript app-router site with source concentrated in `src/app` and `src/components`.
- Confirmed there are no `route.ts` files, no backend/API handlers, and no database client or connection code in the repository.
- Noted that the workspace-level startup files referenced by `AGENTS.md` were not present.
- New task: refactor the repository into a unified SaaS architecture with shared `customers`, `revenue_records`, and `invoices` models, API routes, and a Daytime-first revenue entry flow.
- Implementation target: treat Daytime as the core revenue engine, support mixed revenue items in one record, and generate invoices directly from revenue records.
- Backend hardening requested: replace the process-local singleton with a scalable repository adapter and file-backed local persistence while keeping the API contract stable.
- The backend now persists to a JSON snapshot file with atomic writes and a mutation lock so concurrent creates do not clobber each other.
- `npm run lint` passes for the backend slice.
- Final implementation replaced the landing page with a Daytime-first revenue builder that creates one unified revenue record across vehicle, guide, transfer, and other line items.
- The UI now supports dynamic vehicles and guides, guide time mirroring or custom time, package pricing, overtime pricing, live totals, invoice preview, and direct API-backed actions for create customer, save revenue, and generate invoice.
- Verification completed successfully: `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass. The production build needed one unrestricted run because the sandbox blocked Turbopack from binding a worker port.
- New task: replace the local file-backed storage layer with Supabase persistence while preserving the existing `/api/customers`, `/api/revenue`, and `/api/invoices` route contracts.
- Start point for this change is database schema design plus a new `src/lib/supabase.ts` connection layer.
- Completed the Supabase migration by adding `@supabase/supabase-js`, a server-only admin client in `src/lib/supabase.ts`, and a three-table schema in `supabase/schema.sql`.
- Replaced the file-backed repository with Supabase-backed queries in `src/lib/saas-service.ts`; API routes remained unchanged.
- Added `.env.example` with `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` placeholders.
- Verification completed successfully for the Supabase refactor: `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass. The production build again needed one unrestricted run because sandboxed Turbopack CSS workers cannot bind ports.
- New task: improve Daytime input UX only, specifically faster vehicle/guide add-remove actions, better auto-fill time actions, clearer total emphasis, and fewer clicks without changing the existing layout or styling direction.
- Completed a UX-only Daytime pass without redesigning layout: added duplicate/remove row actions, quick time presets, one-tap guide auto-fill from vehicle, bulk auto-fill for all guides, and stronger live total emphasis.
- Verification for the Daytime UX pass succeeded with `npm run lint` and `npx tsc --noEmit`.
- New task: fix the Supabase integration without changing UI, specifically by validating env vars, checking database connectivity, repairing any API route issues, and testing a `customers` insert.
- Workspace startup files referenced by `AGENTS.md` are still absent except for `memory/memory-state.json` and today's daily log, so task context is being tracked there.
- New task: inspect the existing Supabase/backend integration only and report concrete likely breakpoints around env handling, Supabase client construction, and app-router API paths for `customers`, `revenue`, and `invoices`.
- Supabase env audit showed there is still no `.env.local` in the repo and no `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_URL`, or `SUPABASE_SERVICE_ROLE_KEY` in the current shell environment.
- Patched `src/lib/supabase.ts` to validate env configuration more strictly, allow `SUPABASE_URL` fallback, detect placeholder values, and expose a reusable connection check.
- Patched `src/lib/saas-http.ts` so Supabase config/connection failures return actionable 503 API responses instead of generic 500s.
- Patched `src/lib/saas-service.ts` and `src/app/api/revenue/route.ts` to move revenue filtering into the database and make invoice creation more idempotent by repairing the revenue-record backlink after existing or contended invoice creation.
- Updated `.env.example` to include `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY`.
- Added `scripts/check-supabase.mjs` plus `npm run supabase:check` to load local env files, validate connectivity, insert a temporary `customers` row, and clean it up.
- Verification status for this pass: `npm run lint` and `npx tsc --noEmit` pass.
- Live Supabase verification remains blocked until real credentials are provided: `npm run supabase:check` currently fails immediately with `SUPABASE_URL or NEXT_PUBLIC_SUPABASE_URL is missing.`
- New task: fix `permission denied for schema public` when creating a customer through `POST /api/customers`, explicitly ensuring backend routes use the Supabase service role and leaving the UI unchanged.
- Inspection shows `src/lib/saas-service.ts` already routes writes through `getSupabaseAdminClient()`, so the remaining likely break is that the configured `SUPABASE_SERVICE_ROLE_KEY` is actually anon/invalid or the admin client needs stricter server-side construction and earlier validation.
- Patched `src/lib/supabase.ts` to validate that JWT-based `SUPABASE_SERVICE_ROLE_KEY` values actually carry the `service_role` claim, and to construct the admin client with explicit `public` schema plus `Authorization` and `apikey` headers sourced from the service-role key.
- Patched `src/lib/saas-service.ts` to translate `permission denied for schema public` and related `customers` table permission failures into a clearer Supabase admin-access/config error.
- Patched `src/app/api/customers/route.ts` so `POST /api/customers` validates the backend Supabase connection before attempting the insert.
- Updated `scripts/check-supabase.mjs` to apply the same service-role validation and admin headers as the app runtime.
- Verification for the permission-fix pass: `npm run lint` and `npx tsc --noEmit` pass.
