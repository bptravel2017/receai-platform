# Modules

Each top-level product area has its own module directory so business logic can be added incrementally without coupling features together.

Current module directories are placeholders only.
