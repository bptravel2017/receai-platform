# Learnings

- Keep the Daytime builder aligned to the revenue record contract by including every customer field and by deriving invoice sections from the same `items` array.
- Reusable form shells should accept `className` when they are expected to host layout spacing in multiple positions.
